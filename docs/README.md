# docs

Live design documentation lives here.

- `archive/PLAN/` — the original implementation plans, **deprecated** during the
  flat-layout restructuring. They are kept for historical reference only; the
  current architecture is described in the top-level `README.md`.

Use this folder (not `docs/archive/PLAN/`) for any forward-looking design notes.
