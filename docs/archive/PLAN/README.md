# DEPRECATED — PLAN (archived)

These are the **original implementation plans**, superseded by the codebase
restructuring described in the repo's root `README.md`.

They are kept under `docs/archive/` purely for historical reference. **Do not
treat them as the current design or source of truth** — the canonical feature
schema lives in `src/model_utils.py`, and paths/constants are migrating to
`config.py`.
